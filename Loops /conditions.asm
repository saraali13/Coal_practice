JE ;jump if equal
JG/JNLE ;jump if greater
JL/JNGE ;jump if less
JGE/JNL ;jump if greater and equal
JLE/JNG ;jump if less and equal
JNE ;jump if not equal
  
;for flags
JZ ;if zero
JZN ;if not zero
JC ;if carry
JNC ;if not carry
JO ;if overflow
JNO ;if not overflow
JS ;if signed
JNS ;if not signed
JP ;if parity
JNP ;if not parity
